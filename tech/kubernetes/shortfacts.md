
---

Get a shell within any cluster with `kubectl run tmp-shell --rm -i --tty --image centos -- /bin/bash`

---

Get a shell within any cluster with `kubectl run tmp-shell --rm -i --tty --image centos -- /bin/bash`
